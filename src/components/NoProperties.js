import React from 'react';
import { Typography } from '@material-ui/core';

const NoProperties = () => {
    return <Typography variant="h6">No properties have been found. Please add at least one.</Typography>;
};

export default NoProperties;
